// Deprecated — the strategyFormSettings type was consolidated into the
// `strategyForm` document in schemaTypes/index.js. This file is no longer
// imported and can be deleted once any old strategyFormSettings docs are
// confirmed migrated. Kept as an empty module so existing references in git
// history don't 404 the build.

export {}
